# Server
PORT=3000
NODE_ENV=development

# CORS (comma-separated origins or * for all)
CORS_ORIGIN=http://localhost:5173

# Password Reset Redirect URL (frontend reset password page)
PASSWORD_RESET_REDIRECT_URL=link_to _password_reset_page_in_frontend

# Supabase
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional (recommended for server-side DB operations when RLS is enabled)
# WARNING: Keep this secret and never expose to frontend.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Sentry Error Tracking (Optional)
# Get your DSN from: https://sentry.io → Settings → Projects → [Your Project] → Client Keys (DSN)
SENTRY_DSN=https://your-public-key@o123456.ingest.sentry.io/project-id

# Resend Email Service
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# Admin Email for Support Requests
# This is where contact form submissions will be sent
ADMIN_EMAIL=your-testmail-address@inbox.testmail.app
